const formattedDate = formatDate(new Date())

console.log(`Today's date is ${formattedDate}`)

/*

To convert js file to ts file:
1. Install ts in the root folder
2. Create tsconfig.json with all necessary configuration
3. Change the file extension from .js to .ts
4. Check if everything's fine by compile the ts using "tsc" command

*/